#include <bits/stdc++.h>
using namespace std;
void solve(){
    int h,x;
    cin >> h >> x;
    int tree = 0;
    for(int i=0;i<x;i++) {
        for(int j=0;j<h;j++) {
            
        }
    }
}
int main() {
    cin.tie(nullptr)->ios_base::sync_with_stdio(0);
    int t;
    cin >> t;
    while(t--) {
        solve();
    }
}